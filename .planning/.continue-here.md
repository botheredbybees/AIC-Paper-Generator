---
context: default
phase: arts-health-extension
task: commit
total_tasks: 5
status: in_progress
last_updated: 2026-05-12T07:18:31Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Current State

Two changes to `launch_proposal_writer.py` are complete and all 94 tests pass.
**One task remains: the git commit.**

<current_state>
Changes made, tests passing 94/94. Need to commit exactly two files:
- launch_proposal_writer.py
- tests/test_launch_proposal_writer.py

Do NOT commit the other modified files (blank_icbinb_latex/template.tex,
perform_icbinb_writeup.py, blank_review_latex/, .mcp.json) — they belong to
a different work stream.
</current_state>

<completed_work>
1. `--writeup-type` choices updated: added "review" with help text.
2. `else:` converted to `elif args.writeup_type == "normal":`.
3. New `elif args.writeup_type == "review":` branch added with full tiered-context logic (Tier 1 MCP synthesis, Tier 2 manual PDFs + OA fulltext, Tier 3 S2 abstracts).
4. Top-level `try/except` import of `perform_review_writeup` added so the test patch target `launch_proposal_writer.perform_review_writeup` resolves correctly.
5. All 94 tests pass.
</completed_work>

<remaining_work>
- git add launch_proposal_writer.py tests/test_launch_proposal_writer.py
- git commit -m "feat: add --writeup-type review to launch_proposal_writer.py"
</remaining_work>

<decisions_made>
- perform_review_writeup must be imported at module level (try/except) because
  `patch("launch_proposal_writer.perform_review_writeup")` requires the name
  to exist in the module namespace at patch time. A local import inside the
  elif block is not visible to the patcher.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading

1. `launch_proposal_writer.py` lines 28-40 — top-level try/except import pattern
2. `tests/test_launch_proposal_writer.py` lines 266-301 — the review writeup test

## Infrastructure State

- ml_env virtualenv: `source ~/ml_env/bin/activate`
- MCP server not required for this task
- tectonic shims at ~/bin/pdflatex, ~/bin/bibtex

<next_action>
Run:
  git add launch_proposal_writer.py tests/test_launch_proposal_writer.py
  git commit -m "feat: add --writeup-type review to launch_proposal_writer.py"
</next_action>
